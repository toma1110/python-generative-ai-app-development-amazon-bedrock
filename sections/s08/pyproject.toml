[project]
name = "bedrock-section-08"
version = "0.1.0"
description = "Section 08: evaluate and improve generative AI output"
requires-python = ">=3.11"
dependencies = ["boto3>=1.35,<2"]

[tool.uv]
package = false
