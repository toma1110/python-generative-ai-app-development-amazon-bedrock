[project]
name = "section07-bedrock-file-summary"
version = "0.1.0"
description = "Local file summarization with Amazon Bedrock for Section 07."
requires-python = ">=3.11"
dependencies = ["boto3>=1.40,<2"]

[tool.uv]
package = false
