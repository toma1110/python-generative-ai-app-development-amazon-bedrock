[project]
name = "section06-bedrock-chat"
version = "0.1.0"
description = "CLI chat and conversation history exercises for Amazon Bedrock."
requires-python = ">=3.11"
dependencies = ["boto3>=1.40,<2"]

[tool.uv]
package = false
